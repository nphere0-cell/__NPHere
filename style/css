:root {
  --mist-blue-500: #6fb7ff;
  --mist-blue-700: #2b8eff;
  --accent-pink-500: #ff7fbf;
  --dark-text: #222;
  --light-text: #fefefe;
}
body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  background: #fff;
  color: var(--dark-text);
  overflow-x: hidden;
}
/* Navbar */
nav {
  position: fixed;
  top: 0;
  width: 100%;
  background: #fff;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 2rem;
  z-index: 1000;
}
nav .logo img {
  height: 50px;
  transition: transform 0.3s;
  animation: logoFade 1.2s ease;
}
nav .logo img:hover {
  transform: scale(1.1) rotate(-5deg);
}
nav ul {
  display: flex;
  gap: 1.5rem;
  list-style: none;
  margin: 0;
}
nav ul li a {
  text-decoration: none;
  font-size: 1rem;
  color: var(--dark-text);
  position: relative;
  transition: color 0.3s;
}
nav ul li a::after {
  content: '';
  position: absolute;
  width: 0;
  height: 3px;
  background: var(--accent-pink-500);
  left: 0;
  bottom: -6px;
  transition: 0.3s;
}
nav ul li a:hover::after {
  width: 100%;
}
/* Hero */
header {
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  background: linear-gradient(135deg, var(--mist-blue-500), var(--accent-pink-500));
  color: var(--light-text);
  position: relative;
  overflow: hidden;
}
header .hero-logo img {
  height: 120px;
  margin-bottom: 1.5rem;
  animation: scaleUp 2s ease;
}
header h1 {
  font-size: 4rem;
  margin-bottom: 1rem;
  animation: fadeDown 1.2s ease;
}
header p {
  font-size: 1.5rem;
  font-weight: 300;
  animation: fadeUp 1.4s ease;
}
.cta-btn {
  margin-top: 2rem;
  padding: 1rem 2rem;
  border: none;
  border-radius: 50px;
  background: var(--light-text);
  color: var(--mist-blue-700);
  font-size: 1.2rem;
  cursor: pointer;
  transition: transform 0.3s, background 0.3s;
}
.cta-btn:hover {
  transform: scale(1.1);
  background: var(--accent-pink-500);
  color: var(--light-text);
}
/* Section */
section {
  max-width: 1200px;
  margin: 4rem auto;
  padding: 2rem;
  border-radius: 1.5rem;
  animation: fadeIn 1.5s ease;
}
section h2 {
  font-size: 2.5rem;
  margin-bottom: 2rem;
  text-align: center;
  color: var(--mist-blue-700);
}
.features {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
}
.feature {
  background: rgba(255,255,255,0.9);
  padding: 2rem;
  border-radius: 1rem;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  text-align: center;
  transform: translateY(40px);
  opacity: 0;
  transition: all 0.8s ease;
}
.feature.visible {
  transform: translateY(0);
  opacity: 1;
}
/* Footer */
footer {
  text-align: center;
  padding: 2rem;
  background: linear-gradient(135deg, var(--mist-blue-500), var(--accent-pink-500));
  color: var(--light-text);
}
/* Animations */
@keyframes fadeDown {
  from {opacity:0; transform: translateY(-30px)}
  to {opacity:1; transform: translateY(0)}
}
@keyframes fadeUp {
  from {opacity:0; transform: translateY(30px)}
  to {opacity:1; transform: translateY(0)}
}
@keyframes fadeIn {
  from {opacity:0;}
  to {opacity:1;}
}
@keyframes scaleUp {
  from {opacity:0; transform: scale(0.5)}
  to {opacity:1; transform: scale(1)}
}
@keyframes logoFade {
  from {opacity:0; transform: translateX(-20px)}
  to {opacity:1; transform: translateX(0)}
}
/* About Section */
.about-section {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: center;
  max-width: 1200px;
  margin: 5rem auto;
  padding: 3rem 2rem;
  border-radius: 1.5rem;
  background: linear-gradient(135deg, rgba(111,183,255,0.1), rgba(255,127,191,0.1));
  box-shadow: 0 10px 30px rgba(0,0,0,0.08);
  overflow: hidden;
  transform: translateY(50px);
  opacity: 0;
  transition: all 1s ease;
}
.about-section.visible {
  transform: translateY(0);
  opacity: 1;
}
.about-content h2 {
  font-size: 2.8rem;
  margin-bottom: 1rem;
  color: var(--mist-blue-700);
  position: relative;
}
.about-content p {
  margin-bottom: 1rem;
  font-size: 1.1rem;
  line-height: 1.6;
}
.about-content .highlight {
  color: var(--accent-pink-500);
  font-weight: 600;
}
.learn-btn {
  margin-top: 1rem;
  padding: 0.8rem 1.8rem;
  border: none;
  border-radius: 50px;
  background: var(--mist-blue-500);
  color: var(--light-text);
  font-size: 1rem;
  cursor: pointer;
  transition: transform 0.3s, background 0.3s;
}
.learn-btn:hover {
  transform: scale(1.05);
  background: var(--accent-pink-500);
}
.about-image img {
  width: 100%;
  border-radius: 1rem;
  box-shadow: 0 10px 25px rgba(0,0,0,0.15);
  animation: floatImg 6s ease-in-out infinite;
}
@keyframes floatImg {
  0% { transform: translateY(0px); }
  50% { transform: translateY(-20px); }
  100% { transform: translateY(0px); }
}
